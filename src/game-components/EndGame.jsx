const EndGame = (props) => {
    if (props.isEnd == true) {
        return <div className="backdrop-blur-sm h-full w-full top-0 left-0 block absolute">

        </div>
    }
}

export default EndGame;